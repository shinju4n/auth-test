# AI 모델 설정
# GitHub Actions 워크플로우에서 사용됩니다

# =============================================================================
# Gemini 모델 설정
# =============================================================================
MODEL_NAME="gemini-2.0-flash-exp"
TEMPERATURE="0.7"
MAX_TOKENS="2048"

# =============================================================================
# 코드 리뷰 설정
# =============================================================================
# 심각도 필터 (critical, high만 출력)
SEVERITY_FILTER="critical|high"

# 제외할 파일 패턴 (파이프로 구분)
EXCLUDE_PATTERNS="package-lock.json|yarn.lock|pnpm-lock.yaml|*.min.js|*.min.css|*.map|.env.example|.gitignore|*.md|*.txt|*.log"

# =============================================================================
# AI 답변 설정
# =============================================================================
# 최대 답변 길이 (토큰)
REPLY_MAX_TOKENS="1024"

# 답변 온도 (창의성)
REPLY_TEMPERATURE="0.7"

# =============================================================================
# PR 생성/업데이트 설정
# =============================================================================
# 언어 설정 (KOREAN|ENGLISH)
PR_LANGUAGE="KOREAN"
